{
  "name": "magnetoclip-website",
  "version": "1.0.0",
  "private": true,
  "description": "MagnetoClip static site with Vercel serverless API (Paystack verification, EmailJS license email, Splitforms contact proxy).",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site; no build step required.' && exit 0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
